{
  "name": "s3tosftp",
  "version": "0.0.0",
  "description": "AWS Lambda utility to move files from S3 to SFTP",
  "main": "src/app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --trace-warnings src/local.js",
    "run": "node src/app.js handler"
  },
  "author": "Neil Clack",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.294.0",
    "aws-sdk": "^2.1338.0",
    "dotenv": "^16.0.3",
    "ssh2-sftp-client": "^9.0.4",
    "winston": "^3.8.2"
  }
}
